thresholds: {info_days: 60, warning_days: 30, critical_days: 14}
notify_thresholds: [60, 30, 14, 7, 1]
scan: {timeout_sec: 2, workers: 16, max_cidr_hosts: 256, schedule_hours: 0}
dns_overrides: {"*.lab.local": "127.0.0.1"}
trust: {extra_ca_files: ["lab/certs/root_ca.pem"]}
